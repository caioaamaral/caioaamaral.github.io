<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" ><generator uri="https://jekyllrb.com/" version="4.3.3">Jekyll</generator><link href="/feed.xml" rel="self" type="application/atom+xml" /><link href="/" rel="alternate" type="text/html" /><updated>2024-09-10T00:54:10+00:00</updated><id>/feed.xml</id><title type="html">My GitHub Pages Site</title><subtitle>A description of my site.</subtitle><entry><title type="html">A simple way to use a ROS Humble Docker</title><link href="/v4/a-lua-e-do-brasil/" rel="alternate" type="text/html" title="A simple way to use a ROS Humble Docker" /><published>2024-09-08T07:53:00+00:00</published><updated>2024-09-08T07:53:00+00:00</updated><id>/v4/a-lua-e-do-brasil</id><content type="html" xml:base="/v4/a-lua-e-do-brasil/"><![CDATA[<p>According to the <a href="https://docs.docker.com/get-started/overview/">official documentation</a>, Docker is an open platform for developing, shipping, and running applications. Docker enables you to separate your applications from your infrastructure so you can deliver software quickly. With Docker, you can manage your infrastructure in the same ways you manage your applications. By taking advantage of Docker’s methodologies for shipping, testing, and deploying code quickly, you can significantly reduce the delay between writing code and running it in production.</p>

<p>Now that you know what a docker is and what it is used for, this simple tutorial will show you how to install and deal with a docker, dragging and dropping files inside of a docker from your original operating system.</p>

<h2 id="install-docker-download-image-and-create-a-container">Install Docker, download image and create a container</h2>
<h3 id="install-docker">Install Docker</h3>
<div class="language-terminal highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gp">$</span><span class="w"> </span><span class="nb">sudo </span>apt <span class="nb">install </span>docker.io
</code></pre></div></div>

<h3 id="download-ros-humble-image">Download ROS Humble image</h3>
<div class="language-terminal highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gp">$</span><span class="w"> </span>docker pull osrf/ros:humble-desktop
</code></pre></div></div>

<h3 id="create-a-container-using-the-ros-humble-image">Create a container using the ROS Humble image</h3>
<p>First, you need to create a folder to be the docker <code class="language-plaintext highlighter-rouge">/home</code> folder:</p>
<div class="language-terminal highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gp">$</span><span class="w"> </span><span class="nb">mkdir </span>example_folder
</code></pre></div></div>

<p>Create a container using the folder:</p>
<div class="language-terminal highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gp">$</span><span class="w"> </span>docker run <span class="nt">-it</span> <span class="nt">-v</span> /example_folder:/home osrf/ros:humble-desktop
</code></pre></div></div>

<h2 id="using-your-ros-humble-container">Using your ROS Humble container</h2>

<p>Use this command to know the name of your container (in this case, <code class="language-plaintext highlighter-rouge">exciting_dubinsky</code>):</p>

<div class="language-terminal highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gp">$</span><span class="w"> </span>docker container <span class="nb">ls</span> <span class="nt">-a</span>
<span class="go">CONTAINER ID   IMAGE                     COMMAND                  CREATED       STATUS                   PORTS     NAMES
2dfc7fbc6e21   osrf/ros:humble-desktop   "/ros_entrypoint.sh …"   4 weeks ago   Up About an hour                   exciting_dubinsky
</span></code></pre></div></div>
<p>Start and exec your container:</p>
<div class="language-terminal highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gp">$</span><span class="w"> </span>docker container start exciting_dubinsky
<span class="gp">$</span><span class="w"> </span>docker container <span class="nb">exec</span> <span class="nt">-it</span> exciting_dubinsky bash
</code></pre></div></div>
<p><strong>Inside the docker</strong> terminal, use this command to able manipulate files outside the docker:</p>
<div class="language-terminal highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="gp">#</span><span class="w"> </span><span class="nb">chmod</span> <span class="nt">-R</span> 777 /home
</code></pre></div></div>

<p>That is all, now you can create the workspaces using the folder you created on your original OS and just <code class="language-plaintext highlighter-rouge">colcon build</code> using a docker terminal.</p>

<p>Thanks to use this tutorial and don’t forget, if something is wrong you can contact me on my e-mail or Github account.</p>]]></content><author><name></name></author><category term="v4" /><category term="Robotics" /><category term="docker" /><category term="ros2" /><category term="humble" /><summary type="html"><![CDATA[According to the official documentation, Docker is an open platform for developing, shipping, and running applications. Docker enables you to separate your applications from your infrastructure so you can deliver software quickly. With Docker, you can manage your infrastructure in the same ways you manage your applications. By taking advantage of Docker’s methodologies for shipping, testing, and deploying code quickly, you can significantly reduce the delay between writing code and running it in production.]]></summary></entry></feed>